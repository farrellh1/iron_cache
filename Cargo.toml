[package]
name = "iron_cache"
authors = ["farrellh1"]
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"

[dev-dependencies]
tokio-test = "0.4"
